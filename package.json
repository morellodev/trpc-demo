{
  "name": "trpc-demo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "NODE_ENV=development wsrun --parallel dev"
  },
  "devDependencies": {
    "wsrun": "^5.2.4"
  }
}
